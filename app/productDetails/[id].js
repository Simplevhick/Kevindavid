import React from 'react'
import productData from '@/components/Blogs';

const ProductData = () => {
    const { id } = useParams();

//   const product = productData.((productData) => productData.id == id);

  return (
    <div>ProductData</div>
  )
}

export default ProductData